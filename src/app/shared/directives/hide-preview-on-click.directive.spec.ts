import { HidePreviewOnClickDirective } from './hide-preview-on-click.directive';

describe('HidePreviewOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HidePreviewOnClickDirective();
    expect(directive).toBeTruthy();
  });
});

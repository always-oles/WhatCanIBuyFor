import { TooltipImageDirective } from './tooltip-image.directive';

describe('TooltipImageDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipImageDirective();
    expect(directive).toBeTruthy();
  });
});

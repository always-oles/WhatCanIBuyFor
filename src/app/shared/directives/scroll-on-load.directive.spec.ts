import { ScrollOnLoadDirective } from './scroll-on-load.directive';

describe('ScrollOnLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollOnLoadDirective();
    expect(directive).toBeTruthy();
  });
});

import { OnImageErrorDirective } from './on-image-error.directive';

describe('OnImageErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new OnImageErrorDirective();
    expect(directive).toBeTruthy();
  });
});
